--- a/drivers/leds/Kconfig
+++ b/drivers/leds/Kconfig
@@ -632,6 +632,14 @@ config LEDS_IS31FL32XX
 
 comment "LED driver for blink(1) USB RGB LED is under Special HID drivers (HID_THINGM)"
 
+config LEDS_NU801
+	tristate "LED driver for NU801 RGB LED"
+	depends on LEDS_CLASS
+	depends on GPIOLIB
+	help
+	  This option enables support for NU801 RGB LED driver chips
+	  accessed via GPIO.
+
 config LEDS_BLINKM
 	tristate "LED support for the BlinkM I2C RGB LED"
 	depends on LEDS_CLASS
--- a/drivers/leds/Makefile
+++ b/drivers/leds/Makefile
@@ -74,6 +74,7 @@ obj-$(CONFIG_LEDS_MLXCPLD)		+= leds-mlxc
 obj-$(CONFIG_LEDS_NIC78BX)		+= leds-nic78bx.o
 obj-$(CONFIG_LEDS_MT6323)		+= leds-mt6323.o
 obj-$(CONFIG_LEDS_RESET)		+= leds-reset.o
+obj-$(CONFIG_LEDS_NU801)		+= leds-nu801.o
 
 # LED SPI Drivers
 obj-$(CONFIG_LEDS_DAC124S085)		+= leds-dac124s085.o
